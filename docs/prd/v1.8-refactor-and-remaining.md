# v1.8 Refactor + Remaining Features

## Status
Approved

## Wave 1 — Refactor (code quality)
1. SessionConfig dataclass — replace 25+ kwargs with one config object
2. _recreate_session helper — extract lock-stop-create pattern from 10+ commands
3. bot.py split into Cogs — project_cog, session_cog, cost_cog, tools_cog, agent_cog, mcp_cog
4. _on_retry preserve full config — pass SessionConfig through retry

## Wave 2 — Fixes & Polish
5. Config change warning — ⚠️ "This will reset your conversation" confirmation
6. Env value masking — show **** instead of real values
7. send_to_claude full config — pass env/mcp/budget/add_dirs from project binding
8. Wire PostToolUse/Stop hooks — connect to Discord notifications
9. Subtask depth cap — max 10 levels visual
10. Cache claude --version — store at startup

## Wave 3 — New Features
11. Forum Channel mode — /project set-mode forum|thread
12. Discord Polls for AskUserQuestion — when >10 options or multiSelect
13. Pin important replies — /pin command
14. Rate limit dashboard — /ratelimit show
15. Multi-guild config — per-guild project root
16. User parameter — pass Discord username to Claude
17. Debug mode — /debug toggle for verbose logging
18. Bare mode — /bare toggle for minimal Claude
19. Session naming — /session name <name>
20. Notification settings — /notify on|off for pre-tool messages
